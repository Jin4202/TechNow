/**
 * 사이트 자체에 대한 값 (로드맵 7.1, 7.1a).
 *
 * 환경변수가 아니라 config 인 이유: 도메인은 환경마다 달라지는 값이 아니라
 * 프로젝트가 정한 값이고, canonical 은 로컬에서도 프로덕션 주소를 가리켜야 한다.
 * 환경변수로 빼면 Vercel 과 Trigger.dev 양쪽에 같은 값을 심어야 하고
 * 한쪽을 빠뜨리면 조용히 틀린다.
 */

/**
 * 사이트의 원점(origin). 경로를 붙이지 않는다.
 *
 * **2차 공개에서 커스텀 도메인으로 바뀐다.** 그때 고칠 곳이 여기 하나가 되도록
 * `metadataBase` 와 크롤러 User-Agent(`http.ts`)가 전부 이 값을 읽는다.
 */
export const SITE_URL = 'https://technow-seven.vercel.app';

/**
 * 공개 문의 주소. **없으면 문의 항목 자체를 렌더하지 않는다** (푸터와 About 양쪽).
 *
 * **1차 공개는 비운 채로 나간다** (2026-09-09 사용자 결정). 공개 페이지에 적힌
 * 주소는 수집 봇의 표적이 되고, 한 번 색인되면 나중에 내려도 돌아오지 않는다.
 * 2차에서 도메인이 생기면 그 도메인 주소로 채운다.
 *
 * 비어 있는 동안 **About 의 "한계" 절이 오류 제보를 청하지 않는다.** 알릴 수단을
 * 주지 않으면서 알려달라고 하는 상태를 남기지 않기 위해서다 — 주소를 채울 때
 * 그 문구도 같이 되돌린다.
 *
 * `null` 이 기본인 것은 의도적이다. 가짜 주소(`hello@example.com` 같은)를
 * 넣어두면 그대로 공개될 위험이 있고, 빈 문자열은 링크가 깨진 채 나간다.
 */
export const CONTACT_EMAIL: string | null = null;
