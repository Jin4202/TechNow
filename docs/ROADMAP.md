# ROADMAP

`MASTER_PLAN.md` §5를 실행 가능한 형태로 옮기고, 리뷰에서 빠진 것으로 확인된 태스크를 **[추가]** 로 끼워 넣었다.
원래 번호는 유지하고, 추가분은 `1.1a` 같은 접미사를 쓴다.

**규칙**
- 태스크 하나 = 작업 단위 하나. 완료 기준을 실제로 만족시킨 뒤 다음으로.
- 완료하면 여기 체크박스와 아래 "현재 위치"를 갱신한다.
- Phase 완료 조건을 만족하기 전에 다음 Phase로 넘어가지 않는다.
- 사이트는 Phase 7까지 접근 게이트 뒤에 있다.

## 현재 위치

**Phase 0 완료. Phase 1 미착수.**

- 구조 리뷰 9개 항목 확정 → `DECISIONS.md` D-01~D-09 (`1.0a` 완료)
- Phase 0 실행 중 결정 5건 → `DECISIONS.md` D-10~D-14
- 앱이 basic auth 뒤에서 Vercel에 떠 있고, CI가 typecheck·lint·test·시크릿검사를 돌린다
- Supabase 로컬 스택 기동 중 (스키마는 아직 없음)

다음 할 일: `1.0` 7개 카테고리와 한국어 명칭, 피드 범위, config 기본값을 `DECISIONS.md`에 확정.

---

## Phase 0. 저장소 기반 [추가]

기획서에 없지만 없으면 1.1부터 막히는 것들.

| # | 태스크 | 완료 기준 | |
|---|---|---|---|
| 0.1 | Next.js 16(App Router) + TypeScript + Tailwind 4 초기화, git 저장소 연결 | `pnpm dev` 로 기본 페이지가 뜨고 `origin/main`에 올라감 | ✅ |
| 0.2 | `src/` 디렉터리 구조 (`CLAUDE.md` §3) + config 상수 파일 | 구조가 문서와 일치하고 config 값이 `DECISIONS.md`와 일치 | ✅ |
| 0.3 | 환경변수 규약. `.env.example` 2블록 분리, `env.ts`의 앱/파이프라인 스키마 분리 | 파이프라인 키가 별도 블록·별도 접근자로 격리됨 | ✅ |
| 0.4 | Vitest + lint + typecheck + CI + 시크릿 유출 검사 | 깨뜨린 테스트와 앱 코드의 서비스 키를 CI가 각각 잡음 (실제 확인) | ✅ |
| 0.5 | 접근 게이트 (`src/proxy.ts` basic auth) + Vercel 배포 | 배포본이 자격증명 없이 401, 자격증명으로 200 | ✅ |
| 0.6 | **[추가]** Supabase 로컬 스택 (`supabase init` / `start`) | `supabase status` 정상, `.env.local`이 로컬 스택을 가리킴 | ✅ |
| 0.7 | **[추가]** 문서 갱신 (ROADMAP / CLAUDE.md 커밋 규칙 / DECISIONS) | 실행 중 내린 결정이 `DECISIONS.md`에 기록됨 | ✅ |

**Phase 0 완료 조건**: 빈 앱이 게이트 뒤에 배포되고, CI가 돌고, 폴더 구조가 `ARCHITECTURE.md`와 맞는다. — **충족**

배포: https://technow-seven.vercel.app (basic auth 뒤. `7.7`에서 게이트 제거)

---

## Phase 1. Foundation

| # | 태스크 | 완료 기준 |
|---|---|---|
| 1.0 | `docs/DECISIONS.md`에 7개 카테고리와 한국어 명칭, 피드 범위, config 기본값 추가 (파일은 이미 존재) | 카테고리 enum이 확정되고 "미기록" 표의 해당 행이 지워짐 |
| ~~1.0a~~ | ~~**[추가]** 구조 리뷰 9개 항목 확정~~ | **완료.** `DECISIONS.md` D-01~D-09 |
| 1.1 | Supabase 프로젝트 생성, `MASTER_PLAN` §4 + `ARCHITECTURE` §4 전 테이블 마이그레이션 | 테이블 생성됨 |
| 1.1a | **[추가]** 전 테이블 RLS 활성화 + 정책 (D-02, `ARCHITECTURE` §4 표) | anon 키로 draft 기사·`pipeline_runs`·`source_texts` 조회 시 모두 0행 |
| 1.2 | Supabase Auth 가입 / 로그인 / 로그아웃 | 새 계정으로 로그인·로그아웃 가능 |
| 1.3 | 가입 시 profile 자동 생성 (`locale`, `premium` 기본값) | 가입 후 profile 행이 생김 |
| 1.4 | Trigger.dev 초기화, hello-world 스케줄 태스크 1개 | 대시보드 테스트 버튼과 스케줄 양쪽에서 실행됨 |
| 1.5 | RSS 피드 3~5개 선정 (7개 카테고리 전부 커버) | 각 피드 URL이 응답함 |
| 1.6 | RSS 파서 (title, description, date, URL) — Trigger.dev 태스크 | 한 피드에서 5개 이상 파싱 |
| 1.7 | 전 피드로 확장, 피드별 실패 격리 | 한 피드가 죽어도 나머지가 진행됨 |
| 1.8 | `seen_feed_items` 중복 제거 | 두 번 실행해도 항목당 한 행 |
| 1.8a | **[추가]** `status` 컬럼(pending/processed)과 상태 전이 (D-01) | 신규 항목이 `pending`으로 들어가고, 선정 단계 통과 후 `processed`가 됨 |
| 1.8b | **[추가]** 중간 실패 시 `pending` 유지 + 다음 런 재후보 | 그룹핑 단계에서 강제 실패시키면 다음 실행에서 같은 항목이 다시 후보가 됨 |
| 1.8c | **[추가]** 3일 지난 `pending` 폐기, 90일 지난 `processed` 정리 | 오래된 mock 행이 정리 실행 후 사라짐 |
| 1.9 | 임시 경로: 새 RSS 항목 → placeholder 기사 | 실행하면 articles 테이블이 채워짐 |
| 1.10 | `pipeline_runs` 로깅 | 실행마다 한 행, 카운트 포함 |
| 1.11 | 01:00 America/Los_Angeles 일간 스케줄 | 지정 시각 자동 실행이 로그에 남음 |
| 1.12 | 기사 목록 페이지 (영문 UI) | placeholder 기사가 목록으로 렌더됨 |
| 1.13 | 반응형 스타일, 단순 기본 디자인 | 모바일 폭에서 레이아웃이 유지됨 |

**Phase 1 완료 조건**: 이틀 연속 placeholder 기사가 자동으로 쌓이고, 로그인 사용자와 비로그인 사용자가 같은 목록을 본다.

---

## Phase 2. 토픽 선정

| # | 태스크 | 완료 기준 |
|---|---|---|
| 2.0 | 중요도 루브릭 문서 (`docs/RUBRIC.md`). 3축, 1~5 척도, 레벨별 예시 | 1페이지 루브릭 존재 |
| 2.1 | 저비용 필터 (길이, 홍보성, 키워드 off-topic) | mock 데이터로 검증됨 |
| 2.2 | 그룹핑 프롬프트 (Haiku) | 같은 사건의 두 항목이 한 토픽으로 묶임 |
| 2.3 | Follow-up 감지. 최근 7일 발행 제목을 그룹핑 프롬프트에 투입 | 기발행 기사에 대한 mock 항목이 follow-up으로 표시됨 |
| 2.4 | 1차 점수 프롬프트, 3축 점수 + 근거, structured output (Haiku) | 모든 토픽에 3개 점수와 근거가 생김 |
| 2.5 | 근접 재채점 (트리거 페이지 fetch 후) | 밴드 안 토픽은 재채점, 밖 토픽은 fetch 안 함 |
| 2.6 | 임계 규칙 `총점 ≥ 10 && 모든 축 ≥ 3` (D-09), 일 상한 3, 밴드·윈도우·pending TTL을 config로 | config 변경이 배포 없이 선정 결과를 바꿈 |
| 2.7 | 선정 로깅 → `pipeline_runs` | 실행마다 전 토픽·점수·follow-up 여부·통과 여부가 남음 |
| 2.7a | **[추가]** `topic_hash` 기반 멱등성 | 같은 런을 재시도해도 기사가 중복 생성되지 않음 |
| 2.7b | **[추가]** 선정 종료 시 후보 전체를 `processed`로 갱신 (D-01) | 탈락 항목도 `processed`가 되어 다음 날 재채점되지 않음 |
| 2.8 | 주간 캘리브레이션 리뷰 (3~4주 반복) | 로그 근거로 임계 또는 밴드를 최소 1회 조정 |

**Phase 2 완료 조건**: 원시 항목 100건 규모의 실행이 합리적인 토픽 0~3개를 고르고, follow-up이 정확히 식별되며, 로그만 보고 각 토픽의 통과/탈락 이유를 설명할 수 있다.

---

## Phase 3. 조사와 기사 작성

| # | 태스크 | 완료 기준 |
|---|---|---|
| 3.1 | 하우스 스타일 가이드 (`docs/STYLE_GUIDE.md`) | 1페이지 가이드 존재 |
| 3.2 | Brave Search 클라이언트, 토픽당 호출 상한 | 한 토픽에서 2~3개 쿼리의 링크 결과를 받음 |
| 3.2a | **[추가]** Brave 무료 티어 실제 한도 확인 후 `DECISIONS.md` 기록 | 월 쿼리 한도와 예상 사용량이 문서에 있음 |
| 3.3 | 소스 tier config (Tier1/Tier2/block) + URL 필터 | 혼합 URL 목록이 올바르게 걸러짐 |
| 3.4 | 직접 fetch + Readability 추출, 페이월 감지, robots.txt 확인 | mock 페이월 페이지는 skip, 정상 페이지는 깨끗한 본문 |
| 3.4a | **[추가]** 도메인별 요청 간격 + 식별 User-Agent | 같은 도메인 연속 요청 사이에 지연이 관측됨 |
| 3.5 | 조사 태스크. 쿼리 생성 → 검색 → 필터 → fetch → 5개에서 중단 | 한 토픽이 3~5개 출처 본문을 만들고 `article_sources`에 tier와 함께 저장됨 |
| 3.5a | **[추가]** `source_texts` 테이블에 출처 본문 저장 (D-05) | 다음 단계가 `run_id`/`topic_hash`로 본문을 조회함 |
| 3.5b | **[추가]** 런 종료 시 `expires_at` 만료분 삭제 태스크 | 만료된 mock 행이 런 후 사라짐 |
| 3.6 | 작성 프롬프트, 구조화 JSON + 섹션별 출처 참조 (Sonnet) | 한 토픽이 완성된 기사를 만듦 |
| 3.6a | **[추가]** `body` JSONB 섹션 배열 + `article_sources.ordinal` + zod 검증 (D-03) | 섹션의 `sources`가 존재하지 않는 ordinal을 가리키면 작성 실패로 처리됨 |
| 3.7 | Grounding — 클레임 추출 (**Sonnet**, D-06) | 샘플 기사에서 검증 가능한 클레임 목록이 나옴 |
| 3.8 | Grounding — 클레임 대조 (Sonnet) | 조작한 숫자는 걸리고, 표기만 다른 참값은 안 걸림 |
| 3.8a | **[추가]** 출처 본문 블록 배치 규약 통일 + prompt caching (D-06) | 작성·추출·검증·재작성 4단계가 동일 프리픽스를 쓰고, `cost_cached_tokens`에 적중이 기록됨 |
| 3.8b | **[추가]** 캐시 TTL과 체인 소요 시간 확인 → `DECISIONS.md` 기록 | TTL 안에 4단계가 끝나는지 실측값이 문서에 있음 |
| 3.9 | Grounding 실패 시 재시도·skip 로직 | mock 실패가 1회 재시도 후 skip됨 |
| 3.10 | 최소 출처 수 + Tier 1 규칙 | 출처 2개짜리 토픽은 발행되지 않음 |
| 3.11 | Fixture 세트 구축. 실제 토픽 5~10개 + 캐시된 출처 본문 | 라이브 검색 없이 조사·작성 프롬프트가 돌아감 |
| 3.12 | 파이프라인을 부모/자식 태스크로 분리, 단계별 재시도 | 한 단계 실패가 전체 재실행 없이 재시도됨 |
| 3.13 | 발행 태스크. 필수 자산 config, 06:30 컷오프, 상태 흐름 | 06:30 이후에는 `ready` 승격이 멈추고 `ready_pending`으로 남음 |
| 3.13a | **[추가]** `publish-batch`를 07:00 PT **별도 스케줄**로 분리 (D-04) | 파이프라인 런을 강제 실패시켜도 07:00 발행이 독립 실행되어 `ready` 기사를 내보냄 |
| 3.13b | **[추가]** `held_back_count` 2회 도달 시 `failed` 처리 + 알림 | mock으로 두 번 연속 hold-back 시키면 failed가 되고 알림이 감 |
| 3.14 | 기사 상세 페이지 (출처 섹션, follow-up 링크) | 기사·출처·연결된 이전 기사가 렌더됨 |
| 3.15 | Phase 1 placeholder 경로 제거, 실제 파이프라인으로 교체 | 조사된 기사만 발행됨 |
| 3.15a | **[추가]** 남아 있는 placeholder 기사 정리 | articles 테이블에 placeholder가 없음 |
| 3.16 | 실행당 비용 로깅 (토큰, 검색 호출, fetch 페이지) | `pipeline_runs`에 실행별 비용 추정치가 남음 |
| 3.16a | **[추가]** `cost_fixed` / `cost_variable` 분리 기록 (D-07) | 기사 0개인 날에도 `cost_fixed`가 기록되고, `cost_variable`/기사 수가 0.20 USD 이하 |
| 3.17 | 사람 품질 리뷰. 발행 기사 10개를 스타일 가이드·루브릭과 대조 | 메모가 남고 프롬프트가 최소 1회 수정됨 |

**Phase 3 완료 조건**: 연속 발행된 기사 10개가 원본이고, 3~5개 출처를 인용하고, grounding을 통과하고, 사람이 읽기에 좋고, 비용 로그의 기사당 단가가 목표 이하다.

---

## Phase 4. 한국어와 언어 설정

| # | 태스크 | 완료 기준 |
|---|---|---|
| 4.0 | **[추가]** `/[locale]/articles/[slug]` 라우팅 적용 (D-08) | `/en/...`·`/ko/...`가 각각 동작하고 hreflang이 나감 |
| 4.1 | next-intl 설정, `en`/`ko` 메시지 파일, Pretendard 로드 | UI 문자열이 locale로 전환됨 |
| 4.2 | 프로필 페이지의 언어 설정 + 언어 없는 경로의 리다이렉트 기본값 (D-08) | 변경 시 `locale` 갱신 + `/` 진입 시 해당 locale로 리다이렉트 |
| 4.3 | 번역 프롬프트 (영→한, structured output, Sonnet) | 한 기사가 `article_translations`에 한국어 행을 만듦 |
| 4.3a | **[추가]** 번역본 구조 정합성 검증 (D-03) | 섹션 수 또는 `sources` 배열이 원문과 다른 mock 번역이 실패 처리됨 |
| 4.4 | 번역 태스크를 파이프라인에 편입 | 새 기사에 한국어판이 자동 생성됨 |
| 4.5 | locale별 콘텐츠 조회 | 한국어 사용자가 모든 발행 기사에서 한국어를 봄 |
| 4.6 | 한국어 번역을 필수 자산 config에 추가, 런 내 재시도 + hold-back | mock 실패가 그날 아침 발행에서 기사를 제외시킴 |

**Phase 4 완료 조건**: 언어를 바꾸면 목록·상세 페이지의 모든 문자열과 모든 기사 내용이 바뀐다.

---

## Phase 5. 커버 이미지

| # | 태스크 | 완료 기준 |
|---|---|---|
| 5.1 | 비교 테스트. Flux schnell vs Imagen 4 Fast, 동일 프롬프트 5장씩 | Flux schnell 확정 또는 격차 문서화 후 Imagen 선택 |
| 5.2 | 고정 스타일 프리픽스 | 테스트 이미지 5장이 일관되어 보임 |
| 5.3 | Storage 업로드 + `cover_image_url` 저장 | 이미지가 Supabase Storage에서 서빙됨 |
| 5.4 | 커버 이미지를 필수 자산 config에 추가, 런 내 재시도 + hold-back | mock 실패가 그날 아침 발행에서 기사를 제외시킴 |
| 5.5 | 이미지 태스크를 파이프라인에 편입 | 새 기사에 이미지가 자동 생성됨 |
| 5.6 | 목록·상세 레이아웃에 커버 이미지 | 두 뷰포트에서 이미지가 렌더됨 |

**Phase 5 완료 조건**: 3일 연속 발행된 모든 기사에 커버 이미지가 있다.

---

## Phase 6. 스크랩과 월간 요약

| # | 태스크 | 완료 기준 |
|---|---|---|
| 6.1 | 스크랩 / 해제 버튼 | `scraps`에 행이 생기고 사라짐 |
| 6.2 | 스크랩 폴더 페이지 | 스크랩한 기사를 다시 볼 수 있음 |
| 6.3 | 월간 집계 쿼리 | 특정 월의 사용자 스크랩을 반환 |
| 6.4 | 요약 프롬프트. locale 인지, 카테고리별 그룹 형식 (Sonnet) | 기사 5개로 두 언어의 테스트 요약 생성 |
| 6.5 | 스크랩 100개 이상일 때 카테고리 분할 로직 | 대량 mock 세트로 검증됨 |
| 6.6 | 매월 1일 Trigger.dev 스케줄, 스크랩이 있는 전 사용자 대상 | 스케줄 실행이 로그에 남고, 스크랩 없는 사용자는 건너뜀 |
| 6.7 | 요약 페이지 | 저장된 요약이 표시됨 |
| 6.8 | `premium` 플래그로 게이팅 (기본 on) | 플래그를 끄면 해당 사용자에게 기능이 숨겨짐 |

**Phase 6 완료 조건**: 전월에 스크랩한 모든 사용자에게 요약이 생성된다.

---

## Phase 7. 공개 준비

| # | 태스크 | 완료 기준 |
|---|---|---|
| 7.0 | 최종 앱 이름과 도메인 | 이름 확정, 도메인 등록 |
| 7.1 | 이용약관·개인정보처리방침 페이지 | 푸터에서 링크됨 |
| 7.2 | 인증·스크랩 엔드포인트 rate limiting | 남용 테스트가 차단됨 |
| 7.3 | 카테고리·태그 필터 UI | 필터가 목록을 좁힘 |
| 7.4 | 에러 모니터링 + 배치 실패 알림 | 강제 실패가 알림을 보냄 |
| 7.5 | 월 누적 비용이 예산의 80% 초과 시 알림 | 강제 초과가 알림을 보냄 |
| 7.6 | 사용자 데이터 격리 테스트 | 두 번째 계정이 첫 계정의 스크랩을 볼 수 없음 |
| 7.6a | **[추가]** anon 키로 비공개 데이터 접근 시도 테스트 | draft 기사, 타인 profile, `pipeline_runs` 전부 접근 불가 |
| 7.7 | 접근 게이트 제거 | 비밀번호 없이 사이트 접근 가능 |

---

## 백로그 (계획서상 후순위)

- Batch API 도입 (비용 여유가 필요해질 때)
- 시각 디자인 다듬기
- 월간 요약 유료화 (`premium` 플래그는 이미 있음)
